=begin
#Crossly Buyer API

#The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.

The version of the OpenAPI document: 1.0.0

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 5.4.0

=end

require 'date'
require 'time'

module CrosslyBuyer
  class GetSpatialPublicResponseProfile
    # Matches `crossly_market_categories.slug`, or '*' for the fallback.
    attr_accessor :category_slug

    # Room title, shown in the switcher.
    attr_accessor :label

    # One line of why this space is shaped the way it is. Surfaced in the UI.
    attr_accessor :tagline

    attr_accessor :presentation

    # Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.
    attr_accessor :item_model_variants

    attr_accessor :item_size

    # The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds  cards. The solver walks this to decide what to create next when the  current container fills up.
    attr_accessor :containers

    # Default grouping, in order. Each level becomes a divider or a container.
    attr_accessor :default_group_by

    # Default ordering inside a group.
    attr_accessor :default_sort_by

    # A SEPARATE profile for graded/sealed copies of the same category.    This is not a flourish. Nobody puts a slabbed card in a binder — it does  not fit and it would be vandalism. Graded cards go on a wall, raw cards go  in pockets, and a space that ignores that is immediately wrong to the only  people who would use it. Same for CGC comics.
    attr_accessor :graded_variant

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'category_slug' => :'categorySlug',
        :'label' => :'label',
        :'tagline' => :'tagline',
        :'presentation' => :'presentation',
        :'item_model_variants' => :'itemModelVariants',
        :'item_size' => :'itemSize',
        :'containers' => :'containers',
        :'default_group_by' => :'defaultGroupBy',
        :'default_sort_by' => :'defaultSortBy',
        :'graded_variant' => :'gradedVariant'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'category_slug' => :'String',
        :'label' => :'String',
        :'tagline' => :'String',
        :'presentation' => :'String',
        :'item_model_variants' => :'Array<String>',
        :'item_size' => :'GetSpatialPublicResponseProfileItemSize',
        :'containers' => :'Array<GetSpatialPublicResponseProfileContainers>',
        :'default_group_by' => :'Array<String>',
        :'default_sort_by' => :'Array<String>',
        :'graded_variant' => :'String'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
        :'item_model_variants',
        :'graded_variant'
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `CrosslyBuyer::GetSpatialPublicResponseProfile` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `CrosslyBuyer::GetSpatialPublicResponseProfile`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'category_slug')
        self.category_slug = attributes[:'category_slug']
      end

      if attributes.key?(:'label')
        self.label = attributes[:'label']
      end

      if attributes.key?(:'tagline')
        self.tagline = attributes[:'tagline']
      end

      if attributes.key?(:'presentation')
        self.presentation = attributes[:'presentation']
      end

      if attributes.key?(:'item_model_variants')
        if (value = attributes[:'item_model_variants']).is_a?(Array)
          self.item_model_variants = value
        end
      end

      if attributes.key?(:'item_size')
        self.item_size = attributes[:'item_size']
      end

      if attributes.key?(:'containers')
        if (value = attributes[:'containers']).is_a?(Array)
          self.containers = value
        end
      end

      if attributes.key?(:'default_group_by')
        if (value = attributes[:'default_group_by']).is_a?(Array)
          self.default_group_by = value
        end
      end

      if attributes.key?(:'default_sort_by')
        if (value = attributes[:'default_sort_by']).is_a?(Array)
          self.default_sort_by = value
        end
      end

      if attributes.key?(:'graded_variant')
        self.graded_variant = attributes[:'graded_variant']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @category_slug.nil?
        invalid_properties.push('invalid value for "category_slug", category_slug cannot be nil.')
      end

      if @label.nil?
        invalid_properties.push('invalid value for "label", label cannot be nil.')
      end

      if @tagline.nil?
        invalid_properties.push('invalid value for "tagline", tagline cannot be nil.')
      end

      if @presentation.nil?
        invalid_properties.push('invalid value for "presentation", presentation cannot be nil.')
      end

      if @item_size.nil?
        invalid_properties.push('invalid value for "item_size", item_size cannot be nil.')
      end

      if @containers.nil?
        invalid_properties.push('invalid value for "containers", containers cannot be nil.')
      end

      if @default_group_by.nil?
        invalid_properties.push('invalid value for "default_group_by", default_group_by cannot be nil.')
      end

      if @default_sort_by.nil?
        invalid_properties.push('invalid value for "default_sort_by", default_sort_by cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @category_slug.nil?
      return false if @label.nil?
      return false if @tagline.nil?
      return false if @presentation.nil?
      presentation_validator = EnumAttributeValidator.new('String', ["flat", "slabbed", "boxed", "cased", "spined", "hanging", "standing"])
      return false unless presentation_validator.valid?(@presentation)
      return false if @item_size.nil?
      return false if @containers.nil?
      return false if @default_group_by.nil?
      return false if @default_sort_by.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] presentation Object to be assigned
    def presentation=(presentation)
      validator = EnumAttributeValidator.new('String', ["flat", "slabbed", "boxed", "cased", "spined", "hanging", "standing"])
      unless validator.valid?(presentation)
        fail ArgumentError, "invalid value for \"presentation\", must be one of #{validator.allowable_values}."
      end
      @presentation = presentation
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          category_slug == o.category_slug &&
          label == o.label &&
          tagline == o.tagline &&
          presentation == o.presentation &&
          item_model_variants == o.item_model_variants &&
          item_size == o.item_size &&
          containers == o.containers &&
          default_group_by == o.default_group_by &&
          default_sort_by == o.default_sort_by &&
          graded_variant == o.graded_variant
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [category_slug, label, tagline, presentation, item_model_variants, item_size, containers, default_group_by, default_sort_by, graded_variant].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = CrosslyBuyer.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
