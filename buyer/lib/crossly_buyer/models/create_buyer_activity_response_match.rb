=begin
#Crossly Buyer API

#The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.

The version of the OpenAPI document: 1.0.0

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 5.4.0

=end

require 'date'
require 'time'

module CrosslyBuyer
  class CreateBuyerActivityResponseMatch
    attr_accessor :verdict

    attr_accessor :offer

    # Positive when cheaper. Null when no page price was supplied.
    attr_accessor :saving_cents

    # TRUE means the offer's condition and the page's are comparable. False means we matched the item but not its state — a used Crossly copy against a retailer's new one — and the UI must say so rather than claim a saving.
    attr_accessor :condition_comparable

    # Always false today. Crossly shipping is computed at checkout from the buyer's address, which Scout does not have and should not send. Present so the surface that renders \"before shipping\" is reading a fact rather than hard-coding an assumption that stops being true when we add it.
    attr_accessor :shipping_known

    attr_accessor :catalog

    # Other buyable offers for the SAME item, cheapest first, best excluded.  Deliberately not \"you might also like\". We have no behavioural data to build that from, and inventing it would put unrelated items under a badge whose entire value is that it only ever appears when we have the thing the buyer is actually looking at.  What these ARE is the same product from other sellers, in other conditions, at other prices — which is the choice a buyer on a product page genuinely wants, and the one a single \"cheapest\" result hides.
    attr_accessor :alternates

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'verdict' => :'verdict',
        :'offer' => :'offer',
        :'saving_cents' => :'savingCents',
        :'condition_comparable' => :'conditionComparable',
        :'shipping_known' => :'shippingKnown',
        :'catalog' => :'catalog',
        :'alternates' => :'alternates'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'verdict' => :'String',
        :'offer' => :'CreateBuyerActivityResponseMatchOffer',
        :'saving_cents' => :'Float',
        :'condition_comparable' => :'Boolean',
        :'shipping_known' => :'Boolean',
        :'catalog' => :'GetCatalogLookupResponseCatalog',
        :'alternates' => :'Array<CreateBuyerActivityResponseMatchOffer>'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
        :'saving_cents',
        :'catalog',
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `CrosslyBuyer::CreateBuyerActivityResponseMatch` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `CrosslyBuyer::CreateBuyerActivityResponseMatch`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'verdict')
        self.verdict = attributes[:'verdict']
      end

      if attributes.key?(:'offer')
        self.offer = attributes[:'offer']
      end

      if attributes.key?(:'saving_cents')
        self.saving_cents = attributes[:'saving_cents']
      end

      if attributes.key?(:'condition_comparable')
        self.condition_comparable = attributes[:'condition_comparable']
      end

      if attributes.key?(:'shipping_known')
        self.shipping_known = attributes[:'shipping_known']
      end

      if attributes.key?(:'catalog')
        self.catalog = attributes[:'catalog']
      end

      if attributes.key?(:'alternates')
        if (value = attributes[:'alternates']).is_a?(Array)
          self.alternates = value
        end
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @verdict.nil?
        invalid_properties.push('invalid value for "verdict", verdict cannot be nil.')
      end

      if @offer.nil?
        invalid_properties.push('invalid value for "offer", offer cannot be nil.')
      end

      if @condition_comparable.nil?
        invalid_properties.push('invalid value for "condition_comparable", condition_comparable cannot be nil.')
      end

      if @shipping_known.nil?
        invalid_properties.push('invalid value for "shipping_known", shipping_known cannot be nil.')
      end

      if @alternates.nil?
        invalid_properties.push('invalid value for "alternates", alternates cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @verdict.nil?
      verdict_validator = EnumAttributeValidator.new('String', ["cheaper", "not_cheaper", "no_page_price", "currency_mismatch"])
      return false unless verdict_validator.valid?(@verdict)
      return false if @offer.nil?
      return false if @condition_comparable.nil?
      return false if @shipping_known.nil?
      return false if @alternates.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] verdict Object to be assigned
    def verdict=(verdict)
      validator = EnumAttributeValidator.new('String', ["cheaper", "not_cheaper", "no_page_price", "currency_mismatch"])
      unless validator.valid?(verdict)
        fail ArgumentError, "invalid value for \"verdict\", must be one of #{validator.allowable_values}."
      end
      @verdict = verdict
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          verdict == o.verdict &&
          offer == o.offer &&
          saving_cents == o.saving_cents &&
          condition_comparable == o.condition_comparable &&
          shipping_known == o.shipping_known &&
          catalog == o.catalog &&
          alternates == o.alternates
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [verdict, offer, saving_cents, condition_comparable, shipping_known, catalog, alternates].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = CrosslyBuyer.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
